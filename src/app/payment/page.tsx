import { Alert } from "@mui/material";

const Payment = () => {
    return (
        <Alert variant="filled" severity="error">
            Payment Gateway is not setup yet
        </Alert>
    )
}

export default Payment;